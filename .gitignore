.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pycache__/
*.pyc
*.egg-info/
.DS_Store
.env

# Collected events and rollups are local state, never committed
data/
*.duckdb

# Built React bundle — rebuild with: cd frontend && npm install && npm run build
frontend/dist/
frontend/node_modules/
frontend/*.tsbuildinfo

# Terraform: provider binaries and state never belong in git. Lock files DO.
.terraform/
*.tfstate
*.tfstate.*
